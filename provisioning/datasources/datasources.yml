apiVersion: 1

datasources:
  - name: 'foxglove'
    type: 'foxglovedev-foxglove-datasource'
    access: proxy
    isDefault: false
    orgId: 1
    version: 1
    editable: true
    jsonData:
      baseUrl: ${FOXGLOVE_API_BASE_URL}
    secureJsonData:
      apiKey: ${FOXGLOVE_API_KEY}
